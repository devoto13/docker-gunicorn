#!/bin/bash

echo "Starting $APP_NAME as `whoami`"
exec gunicorn $APP_NAME.wsgi:application \
  --name $APP_NAME \
  --workers $NUM_WORKERS \
  --user=app \
  --chdir=/home/app/ \
  --bind=127.0.0.1:8080 \
  --log-level=debug \
  --log-file=-
