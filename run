#!/bin/bash

exec gunicorn $APP_WSGI \
  --name $APP_NAME \
  --bind=0.0.0.0:80 \
  --workers $APP_WORKERS \
  --pythonpath=/root/app/ \
  --access-logfile=/root/app/log/gunicorn_access.log \
  --error-logfile=/root/app/log/gunicorn_error.log
